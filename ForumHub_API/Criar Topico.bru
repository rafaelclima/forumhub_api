meta {
  name: Criar Topico
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/topico
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "titulo":"Pedido de solicitação",
    "mensagem": "todos os pedidos para o email ..."
  }
}

settings {
  encodeUrl: true
}
